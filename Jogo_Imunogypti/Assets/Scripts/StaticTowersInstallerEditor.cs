using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;
/*
[CustomEditor(typeof(StaticTowersInstallerScript))]
public class StaticTowersInstallerEditor : Editor
{
    public override void OnInspectorGUI()
    {
        DrawDefaultInspector();

        StaticTowersInstallerScript myScript = (StaticTowersInstallerScript)target;

        if(GUILayout.Button("Install Celula Dendritica"))
        {
            myScript.InstallDendritica();
        }

        if(GUILayout.Button("Install Cell"))
        {
            myScript.InstallCell();
        }
    }
}*/
